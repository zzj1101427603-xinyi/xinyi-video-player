name: Build Check

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: List files
      run: ls -la
    
    - name: Check Node.js
      run: node --version
    
    - name: Create success message
      run: |
        echo "✅ 项目代码验证成功！" > message.txt
        echo "项目包含以下文件：" >> message.txt
        ls -la >> message.txt
        echo "" >> message.txt
        echo "🎉 现在可以进行APK构建了！" >> message.txt
    
    - name: Upload result
      uses: actions/upload-artifact@v3
      with:
        name: result
        path: message.txt
