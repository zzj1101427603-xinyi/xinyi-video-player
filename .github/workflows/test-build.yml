name: Test Basic Build

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检查代码
      run: echo "步骤1：开始检查代码..."
    
    - name: 📂 列出所有文件
      run: |
        echo "步骤2：列出文件..."
        ls -la
    
    - name: 📦 检查Node.js
      run: |
        echo "步骤3：检查Node.js..."
        node --version
        npm --version
    
    - name: ✅ 完成测试
      run: |
        echo "步骤4：测试完成！"
        echo "如果看到这条消息，说明基础环境正常"
